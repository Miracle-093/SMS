{
  "buildCommand": "npm install && npm run build --workspace packages/shared-types && npm run build --workspace apps/portal",
  "outputDirectory": "apps/portal/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
