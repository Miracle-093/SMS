{
  "buildCommand": "npm install && npm run build --workspace packages/shared-types && npm run build --workspace apps/desktop",
  "outputDirectory": "apps/desktop/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
